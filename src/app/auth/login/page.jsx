export const metadata = {
    title: "Iniciar sesión",
    description: "Clone Produnt Hunt",
};

const login = () => {
    return (
        <div>
            <h1>Login</h1>
        </div>
    );
};

export default login;
